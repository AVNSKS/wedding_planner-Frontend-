import React from 'react';
import Vendors from './Vendors';

// For now, Bookings just reuses the Vendors page UI.
// Later, you can split them if you want a different layout.
const Bookings = () => {
  return <Vendors />;
};

export default Bookings;
